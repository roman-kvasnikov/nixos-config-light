{
  programs.caelestia = {
    enable = true;

    cli.enable = true;

    # systemd = {
    #   enable = false; # если запускаешь из Hyprland через exec-once
    # };

    # settings = {
    #   paths.wallpaperDir = "~/Pictures/Wallpapers";
    # };
  };
}
